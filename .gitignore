# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Index (generated — re-run tools/indexer.py)
.index/pages.json
.index/pages.js

# Embeddings (generated — re-run tools/embedder.py, ~50 MB)
.index/embeddings.json

# IDE / Tauri build artifacts
ide/src-tauri/target/
ide/node_modules/
ide/dist/
ide/.tauri/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
*.swp
*.swo

# Secrets / env
.env
.env.local
*.key

# AI session data (Claude Code memory, plans, session logs)
.claude/

# Old UI prototypes (to be migrated to vault/)
.last/

# Personal vault content
vault/Interaction Log/

# Local settings
ide/src-tauri/gen/schemas/windows-schema.json

# Atlas runtime artefacts
.ide-state.json
.mcp-queue.json
.vault-trash/
.uploads/

# Editor / IDE scratch files
*.bak
*.orig
*.rej

# Third-party module bundles (Flowise, codegraph) — root-only.
# A bare `modules/` would also match ide/src/modules/, ide/src-tauri/src/modules/, etc.
/modules/

# Separate sub-projects at repo root (not part of Atlas OS core)
/threadmind/
/prototypes/
