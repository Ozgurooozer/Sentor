/** Focused-mode bottom bar height in logical (CSS) pixels. */
export const BAR_HEIGHT = 148;
/** Focused-mode bottom bar height when collapsed — only the header row visible. */
export const BAR_HEIGHT_COLLAPSED = 36;

/** Canvas zoom factor applied per scroll tick (8% in/out). */
export const CANVAS_ZOOM_STEP = 1.08;

/** Canvas viewport scale limits. */
export const CANVAS_MIN_SCALE = 0.25;
export const CANVAS_MAX_SCALE = 3.0;

/** Minimum dimensions for a canvas panel node. */
export const CANVAS_PANEL_MIN_W = 240;
export const CANVAS_PANEL_MIN_H = 160;
